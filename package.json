{
  "name": "cursor-ai-file-trigger",
  "displayName": "Cursor AI File Trigger",
  "description": "ファイルの変更をトリガーとしてAIプロンプトを実行するCursor拡張機能",
  "version": "0.0.1",
  "publisher": "your-name",
  "engines": {
    "cursor": ">=0.20.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cursor-ai-file-trigger.runPromptOnFileChange",
        "title": "Run AI Prompt on File Change"
      }
    ]
  },
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.101.0",
    "typescript": "^5.0.0",
    "vscode": "^1.1.37"
  }
}
